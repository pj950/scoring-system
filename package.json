{
  "private": true,
  "description": "Dependencies for the Vercel serverless function backend.",
  "scripts": {
    "build": "echo 'Vercel handles the build process automatically.'"
  },
  "dependencies": {
    "@vercel/postgres": "^0.9.0",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "@vercel/node": "^3.0.25"
  }
}
